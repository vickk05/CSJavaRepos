import java.util.Scanner;

public class Practice
{

	public static void main(String[] args)
	{
		Scanner keyboardinput = new Scanner(System.in);
		// What is happening for each line?
		double randomDouble = Math.random();
		int randomInt = (int) Math.random();
		randomInt = (int) (Math.random() * 100);
		randomInt = (int) (Math.random() * 100 + 1);

		char middleInitial = 'M';
		int charAsciiValue = (int) 'M';
		String firstName = "Victor";
		int stringLength = firstName.length();
		char firstInitial = firstName.charAt(0);

		Character.isDigit(someChar);

		System.out.println("Enter one character!");
		String = input.next();
		char someChar = someChar.charAt(0);
		System.out.println("Character entered =" + someChar);

	}

}
